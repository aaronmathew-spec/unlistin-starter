# --- Required (from Supabase) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# If available, preferred for server-side DB calls (more reliable than anon)
# Either provide one of these; code supports both names:
SUPABASE_SERVICE_ROLE=
SUPABASE_SERVICE_ROLE_KEY=

# --- Recommended ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Enterprise API / Security ---
PAT_HASH_SALT=changeme-long-random
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Webhook sender (no extra env required; uses fetch)

# --- AI / Agents ---
# OpenAI key (used by agents, drafts, policy synthesis, etc.)
OPENAI_API_KEY=

# --- Proofs / Crypto (optional; used by proofs endpoints if enabled) ---
# Hex-encoded Ed25519 private key for signing Merkle roots (dev only; prod -> HSM/KMS)
PROOF_SIGNING_KEY=

# --- Vercel KV (optional; if you later enable KV-backed orchestrator state) ---
KV_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Proof signing (we’ll wire real Ed25519/HSM in Phase 4.2)
PROOF_SIGNING_KEY=

# (Playwright runs on Vercel automatically; no extra env needed)

# --- SMTP (optional; email dispatch gracefully simulates when absent) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="UnlistIN <no-reply@unlistin.app>"

# --- Playwright (optional: CI tuning) ---
# PLAYWRIGHT_BROWSERS_PATH=0
