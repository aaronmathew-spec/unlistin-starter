// app/{{slice}}/page.tsx
export default function {{Slice}}Page() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">{{Slice}}</h1>
      <p className="text-gray-600 mt-2">Scaffolded page for <code>/{{slice}}</code>.</p>
    </div>
  );
}
